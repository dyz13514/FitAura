<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 选中状态（动态设置） -->
    <item android:state_selected="true">
        <shape android:shape="rectangle">
            <corners android:radius="10dip"/>
            <padding
                android:left="10dp"
                android:top="10dp"
                android:right="10dp"
                android:bottom="10dp"/>
            <stroke
                android:width="5dp"
                android:color="#dcdcdc"/>
            <solid android:color="@color/colorAccent"/> <!-- 点击时的颜色 -->
        </shape>
    </item>
    <!-- 默认状态 -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="10dip"/>
            <padding
                android:left="10dp"
                android:top="10dp"
                android:right="10dp"
                android:bottom="10dp"/>
            <stroke
                android:width="5dp"
                android:color="#dcdcdc"/>
            <solid android:color="#ffffff"/> <!-- 默认颜色 -->
        </shape>
    </item>
</selector>